**APIs RESTful** : Interfaces de programmation d'applications qui respectent les principes de conception REST.

**APIs web** : Interfaces de programmation d'applications permettant la communication entre différentes applications web.

**Architecture de Django** : La structure de conception de Django, souvent décrite par le modèle MTV (Model-Template-View).

**Attributs (POO)** : Caractéristiques ou données associées à une classe ou un objet en programmation orientée objet.

**Authentification utilisateur** : Processus de vérification de l'identité d'un utilisateur, notamment dans les applications Django avec des formulaires de login.

**Authorization (En-têtes HTTP)** : En-tête HTTP utilisé pour transmettre des informations d'authentification ou des jetons de sécurité.

**Autorisation utilisateur** : Processus qui détermine les droits d'accès ou les actions qu'un utilisateur authentifié est autorisé à effectuer, géré par les permissions Django.

**Avantages et inconvénients de Flask** : Points forts et faibles de Flask comparés à Django, pertinents pour le choix du framework selon le cas d'utilisation.

**Bases de données NoSQL** : Type de bases de données non relationnelles, incluant les types document (comme MongoDB), clé-valeur et colonne large, avec lesquelles on peut interagir via `pymongo`.

**Bases de données relationnelles** : Type de bases de données organisées en tables reliées par des clés, utilisant SQL, comme `sqlite3`.

**Bases de données SQL** : Type de bases de données relationnelles utilisées en Python via des modules comme `sqlite3` ou des ORM comme SQLAlchemy.

**Bases de Python** : Concepts fondamentaux de Python 3.14 incluant variables, types de données, opérateurs, structures de contrôle et fonctions.

**Bonnes pratiques du développement web** : Ensemble de règles et de conventions à respecter pour un développement web efficace et maintenable, incluant les conventions RESTful.

**Classes (POO)** : Modèles ou plans pour la création d'objets, définissant leurs attributs et leurs méthodes en programmation orientée objet.

**Codes de statut (HTTP)** : Nombres à trois chiffres (ex: 200 OK, 404 Not Found) indiquant le résultat d'une requête HTTP.

**Concepts clés (du Web)** : Idées fondamentales qui définissent le fonctionnement du World Wide Web.

**Consommation de l'API Flask** : Interaction avec une API Flask depuis un client, par exemple un script Python utilisant la bibliothèque `requests`.

**Consommation d'APIs externes** : Utilisation d'APIs tierces depuis une application, comme une application Django avec la bibliothèque `requests`.

**Content-Type (En-têtes HTTP)** : En-tête HTTP spécifiant le type de média des données envoyées dans le corps d'une requête ou d'une réponse (ex: application/json).

**Conventions REST** : Principes de conception pour la création d'APIs web permettant une communication standardisée et stateless.

**Création d'APIs web (Flask)** : Implémentation d'interfaces de programmation d'applications avec Flask, gérant des requêtes HTTP (GET, POST, PUT, DELETE).

**Création d'une application (Django)** : Génération d'un composant modulaire spécifique (comme un blog, un e-commerce) au sein d'un projet Django.

**Création d'un projet (Django)** : Mise en place de la structure de base et de la configuration initiale d'une application Django.

**Création de tables (SQL)** : Définition de la structure des données dans une base de données relationnelle via des commandes SQL.

**DELETE (Requêtes HTTP)** : Méthode HTTP utilisée pour supprimer une ressource spécifique sur le serveur.

**Développement Web avec Django** : Processus de construction de sites web et d'APIs REST complets en utilisant le framework Django.

**Développement Web avec Flask** : Processus de création d'applications web et d'APIs RESTful avec le micro-framework Flask.

**Dict (Types de données)** : Type de données Python représentant une collection non ordonnée de paires clé-valeur (dictionnaire).

**Django** : Framework web Python full-stack qui suit la philosophie DRY et le modèle MTV, utilisé pour créer des sites web et des APIs.

**Django REST Framework (DRF)** : Extension de Django qui facilite la création d'APIs RESTful grâce à des sérialiseurs et des vues d'API.

**DRF (Sérialiseurs)** : Classes de Django REST Framework qui convertissent les objets Python complexes (modèles) en formats de données transférables (JSON) et vice-versa.

**DRF (Vues d'API)** : Composants de Django REST Framework qui définissent la logique de traitement des requêtes HTTP pour les ressources d'une API.

**Écosystème (Flask)** : Ensemble des extensions, bibliothèques et outils qui complètent le micro-framework Flask.

**Encapsulation (POO)** : Principe de la POO consistant à regrouper les données et les méthodes qui les manipulent au sein d'une classe, contrôlant l'accès externe à ces données.

**En-têtes HTTP** : Informations supplémentaires incluses dans les requêtes et réponses HTTP, comme Host, User-Agent, Content-Type, Authorization.

**Environnements virtuels (Python)** : Environnements isolés pour gérer les dépendances de projets Python, évitant les conflits de versions des paquets.

**Float (Types de données)** : Type de données Python représentant des nombres à virgule flottante.

**Fonctions (Python)** : Blocs de code réutilisables en Python, définis avec des arguments, pouvant retourner des valeurs, incluant les fonctions `lambda`.

**Fondamentaux du Web** : Les bases du World Wide Web, son historique et ses concepts clés, incluant le protocole HTTP.

**For (Structures de contrôle)** : Instruction de contrôle de flux en Python utilisée pour itérer sur une séquence d'éléments.

**Formulaires de login/logout (Django)** : Interfaces utilisateur dans Django permettant aux utilisateurs de s'authentifier (se connecter) et de se déconnecter.

**Formulaires HTML** : Éléments HTML utilisés pour collecter des entrées utilisateur sur une page web.

**Full-stack framework (Django)** : Un framework web complet comme Django, qui fournit des outils pour toutes les couches d'une application (du frontend au backend).

**GET (Requêtes HTTP)** : Méthode HTTP utilisée pour récupérer des données d'une ressource spécifiée, sans effet secondaire sur le serveur.

**Gestion des permissions (Django)** : Mécanisme de Django pour contrôler les accès et les actions des utilisateurs ou groupes via des rôles (staff, admin, normaux).

**Gestion des sessions (Flask)** : Mécanisme dans Flask pour maintenir des informations d'état entre les requêtes HTTP d'un même utilisateur.

**Héritage (POO)** : Mécanisme de la POO permettant à une classe d'acquérir les attributs et méthodes d'une autre classe (sa classe parente).

**Historique (du Web)** : L'évolution et le développement du World Wide Web depuis sa création.

**Host (En-têtes HTTP)** : En-tête HTTP spécifiant le nom de domaine du serveur cible de la requête.

**Idempotence (HTTP)** : Propriété d'une méthode HTTP (comme GET, PUT, DELETE) qui signifie que son exécution multiple produit le même résultat qu'une seule exécution.

**If/elif/else (Structures de contrôle)** : Instruction conditionnelle en Python pour exécuter différents blocs de code selon des conditions spécifiées.

**Insertion (SQL)** : Opération SQL pour ajouter de nouvelles lignes de données dans une table de base de données.

**Installation de Django** : Processus de configuration de l'environnement nécessaire pour développer des applications avec Django.

**Installation de Flask** : Processus de configuration de l'environnement nécessaire pour développer des applications avec Flask.

**Installation de Python** : Processus de mise en place de l'interpréteur Python 3.14 et de ses outils sur un système.

**Int (Types de données)** : Type de données Python représentant des nombres entiers.

**Introduction au Web** : Présentation des concepts clés, de l'historique et du fonctionnement du World Wide Web.

**Jinja2** : Moteur de templates utilisé avec Flask pour générer dynamiquement du contenu HTML.

**Lambda (Fonctions Python)** : Fonctions anonymes et de petite taille en Python, souvent utilisées pour des opérations courtes et simples.

**List (Types de données)** : Type de données Python représentant une collection ordonnée et modifiable d'éléments (liste).

**Messages flash (Flask)** : Messages éphémères affichés aux utilisateurs dans une application Flask, souvent utilisés pour des retours d'actions.

**Méthodes (HTTP)** : Verbes HTTP (GET, POST, PUT, DELETE) indiquant l'action à effectuer sur une ressource.

**Méthodes (POO)** : Fonctions associées à une classe ou un objet, définissant son comportement en programmation orientée objet.

**Micro-framework (Flask)** : Framework web léger et minimaliste comme Flask, fournissant les fonctionnalités essentielles pour le développement d'applications web.

**Migrations (Django)** : Mécanisme de Django pour gérer les changements de structure de la base de données (schéma) en fonction des modifications apportées aux modèles.

**Mise à jour (SQL)** : Opération SQL pour modifier des données existantes dans une table de base de données.

**Modélisation de données** : Processus de conception de la structure des données pour une base de données, souvent appliqué à des modèles simples.

**Modèles Django (ORM)** : Classes Python dans Django qui définissent la structure des données de l'application et permettent d'interagir avec la base de données via l'ORM intégré de Django.

**MongoDB** : Base de données NoSQL orientée document, utilisée en Python via la bibliothèque `pymongo`.

**MTV (Model-Template-View) (Django)** : Architecture de conception de Django où le Modèle gère les données, le Template l'affichage, et la Vue la logique métier.

**Objets (POO)** : Instances concrètes d'une classe, possédant des attributs et des méthodes définis par leur classe.

**Opérateurs (Python)** : Symboles utilisés pour effectuer des opérations sur des valeurs et des variables (arithmétiques, de comparaison, logiques, etc.).

**ORM (Object-Relational Mapper)** : Outil permettant d'interagir avec des bases de données relationnelles en utilisant des objets Python plutôt que des requêtes SQL brutes (comme SQLAlchemy ou l'ORM de Django).

**Pages web** : Ensemble de documents HTML et de ressources connexes accessibles via internet, créés avec des frameworks comme Flask et Django.

**Pages web interactives avec Flask** : Pages web dynamiques construites avec Flask et Jinja, capables de réagir aux actions de l'utilisateur.

**Passage de données au template (Jinja2)** : Action d'envoyer des informations depuis le code Python d'une application Flask vers un template Jinja2 pour l'affichage dynamique.

**Persistance des Données** : Capacité d'une application à stocker et à récupérer des données de manière durable, généralement via une base de données.

**Philosophie DRY (Don't Repeat Yourself)** : Principe de développement qui vise à réduire la répétition de code et d'informations, favorisé par Django.

**Polymorphisme (POO)** : Capacité des objets de différentes classes à être traités de manière uniforme si elles partagent une interface commune, en programmation orientée objet.

**POST (Requêtes HTTP)** : Méthode HTTP utilisée pour envoyer des données à une ressource, souvent pour créer une nouvelle ressource ou soumettre des données de formulaire.

**Principes de conception d'API REST** : Lignes directrices pour concevoir des APIs web qui sont stateless, client-serveur, cacheables et utilisent une interface uniforme.

**Programmation Orientée Objet (POO)** : Paradigme de programmation basé sur les concepts de classes et d'objets, incluant attributs, méthodes, héritage, encapsulation et polymorphisme.

**Protocole HTTP** : Protocole fondamental du World Wide Web pour les échanges de données, détaillant les requêtes, les réponses, les ressources et les en-têtes.

**PUT (Requêtes HTTP)** : Méthode HTTP utilisée pour remplacer complètement une ressource existante par de nouvelles données fournies dans la requête.

**Pymongo** : Bibliothèque Python permettant d'interagir avec les bases de données MongoDB (connexion, insertion, recherche, mise à jour, suppression de documents).

**Python 3.14** : Version spécifique du langage de programmation Python, utilisée comme base pour le développement web et la persistance des données.

**Python client (`requests`)** : Un script ou une application Python qui utilise la bibliothèque `requests` pour envoyer des requêtes HTTP et interagir avec des APIs web.

**Récupération de données (SQL)** : Opération SQL pour extraire des informations d'une ou plusieurs tables de base de données.

**Rendu de templates (Jinja2)** : Processus par lequel le moteur Jinja2 combine un fichier template HTML avec des données pour générer le contenu HTML final à afficher.

**Request object (Flask)** : Un objet global dans Flask qui contient toutes les informations de la requête HTTP entrante (données de formulaire, en-têtes, etc.).

**Requêtes HTTP** : Messages envoyés par un client (ex: navigateur) à un serveur pour demander une action ou une ressource (GET, POST, PUT, DELETE).

**Réponses HTTP (Flask)** : Objets renvoyés par les vues Flask, souvent au format JSON, en réponse à une requête HTTP.

**Réponses (HTTP)** : Messages renvoyés par un serveur HTTP en réponse à une requête, incluant un code de statut et des données.

**Ressources (URL)** : Éléments identifiables sur le Web, comme un document, une image ou une API, accessibles via une URL (Uniform Resource Locator).

**Rôles (Django)** : Catégories d'utilisateurs dans Django (ex: staff, admin, utilisateurs normaux) avec des ensembles de permissions spécifiques.

**Routing (Flask)** : Mécanisme de Flask qui associe une URL spécifique à une fonction Python (appelée 'vue') qui traite la requête.

**Set (Types de données)** : Type de données Python représentant une collection non ordonnée d'éléments uniques (ensemble).

**Site web complet (Django)** : Un site web qui inclut à la fois des pages web traditionnelles et des APIs REST, construit avec Django.

**Sites web** : Ensemble de pages web et de ressources connexes accessibles via internet, créés avec des frameworks comme Flask et Django.

**Sqlite3** : Module Python pour interagir avec des bases de données SQLite, une base de données relationnelle embarquée et légère.

**SQLAlchemy** : Bibliothèque Python agissant comme un ORM (Object-Relational Mapper) ou une interface SQL pour interagir avec des bases de données relationnelles.

**Str (Types de données)** : Type de données Python représentant une chaîne de caractères (texte).

**Structures de contrôle (Python)** : Instructions Python comme `if/elif/else`, `for` et `while` qui gèrent le flux d'exécution du programme.

**Structure de projet Flask** : Organisation standardisée des fichiers et dossiers d'une application Flask.

**Templates Django** : Fichiers HTML avec une syntaxe spécifique à Django pour l'intégration de données dynamiques.

**Templates HTML (Jinja2)** : Fichiers HTML qui contiennent des marqueurs spéciaux traités par le moteur Jinja2 pour générer du contenu dynamique pour les pages web Flask.

**Traitement des entrées utilisateur (Flask)** : Opération de lecture et de validation des données soumises par l'utilisateur via des formulaires HTML dans une application Flask, à l'aide de l'objet `request`.

**Tuple (Types de données)** : Type de données Python représentant une collection ordonnée et immuable d'éléments (tuple).

**Types de données (NoSQL)** : Catégories de bases de données NoSQL : document, clé-valeur, colonne large.

**Types de données (Python)** : Catégories de valeurs que Python peut manipuler : `int`, `float`, `str`, `list`, `dict`, `tuple`, `set`.

**URL (Uniform Resource Locator)** : Adresse unique qui identifie une ressource spécifique sur le World Wide Web.

**URLs (Django)** : Système de routage de Django qui mappe les chemins d'URL aux vues appropriées de l'application.

**User-Agent (En-têtes HTTP)** : En-tête HTTP identifiant le logiciel client (navigateur, application) qui envoie la requête.

**Utilisateurs Django** : Système intégré à Django pour gérer les utilisateurs, leur authentification et leurs permissions.

**Variables (Python)** : Noms symboliques qui référencent des valeurs en mémoire dans un programme Python.

**Vues (Django)** : Fonctions ou classes Python dans Django qui reçoivent une requête HTTP, exécutent la logique métier et renvoient une réponse HTTP.

**Vues (Flask)** : Fonctions Python dans Flask associées à des routes spécifiques, qui génèrent la réponse HTTP pour une requête donnée.

**While (Structures de contrôle)** : Instruction de contrôle de flux en Python pour exécuter un bloc de code tant qu'une condition est vraie.

